#!/usr/bin/env ruby

Dir[Dir.pwd + "/lib/*.rb"].each {|lib_file| require lib_file}

def posts
  @posts ||= []
end

Dir[Dir.pwd + "/posts/*.yml"].each { |post| posts << Post.create_from_file(post) }

BlogEngine.set :blog, Blog.new(*posts)
BlogEngine.run!
